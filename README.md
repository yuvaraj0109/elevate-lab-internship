# Semantic HTML Personal Profile

This project is a simple personal profile web page built using semantic HTML elements.

## Features
- Uses semantic tags like header, nav, section, article, and footer
- Accessible image with alt attribute
- Internal navigation using anchor links
- Clean and readable HTML structure

## Tools Used
- HTML5
- VS Code
- Google Chrome
